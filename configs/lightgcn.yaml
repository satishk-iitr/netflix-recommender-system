# LightGCN — Light Graph Convolutional Network (PyTorch Geometric)
# =================================================================
model: lightgcn

params:
  embed_dim: 64
  n_layers: 3
  lr: 0.001
  weight_decay: 0.00001
  epochs: 50
  batch_size: 2048
  reg_weight: 0.0001
  random_state: 42
